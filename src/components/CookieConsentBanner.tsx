/**
 * Cookie consent banner + settings modal.
 * Re-exports CookieConsent for backwards compatibility.
 * Wrap the app in CookieConsentProvider and render <CookieConsent /> (or this component) once.
 */
export { default } from './CookieConsent';
